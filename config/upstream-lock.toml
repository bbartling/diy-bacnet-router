# This is an evidence lock, not yet a Cargo dependency. The first integration
# ticket must replace the short historical value with the currently audited,
# full 40-character dev SHA and then pin every rusty-bacnet crate to that SHA.
[rusty_bacnet]
repository = "https://github.com/jscott3201/rusty-bacnet"
branch_audited = "dev"
revision = "af4e886"
status = "historical-vibe13-pin-needs-current-full-sha-audit"

[vibe13]
repository = "https://github.com/bbartling/py-bacnet-stacks-playground"
path = "vibe_code_apps_13"
evidence = "Phase 1 and Phase 2 Gates 1-4/4b; PR 127 merged per operator report"

