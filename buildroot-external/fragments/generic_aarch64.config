# Optional aarch64 virt tweaks layered on qemu_aarch64_virt_defconfig.
# Keep portable: QEMU virt machine, not a named SBC flash image.
BR2_TARGET_GENERIC_HOSTNAME="diy-bacnet-router"
BR2_TARGET_GENERIC_ISSUE="diy-bacnet-router appliance (generic_aarch64)"
